{title:'Canvas 教程 1 ———— 介绍HTML5 Canvas',content:'<p>HTML5 CANVAS 是一个可以使用JavaScript操纵的实时位图区域。HTML5 CANVAS可以使用Canvas API在每一帧时重绘Canvas，做为一个程序员，你的目标是Canvas在每一帧重绘前，渲染设置好Canvas的每一个像素怎么显示。</p><p>这点让HTML5 Canvas 有别与Flash，Silverlight，或是SVG。在之前的模式中，可以有一个个显示对象被存为图像的绘制者，然后根据对象里的属性来绘制图形，这种绘制方法让绘制者远离底层图像操作，让开发者更少接触到最后渲染的图像。</p><p>基本基于2d的HTML5 Canvas API允许程序员们绘制图形，渲染文字，显示并处理图片，你可以将颜色，变形，透明处理放置到Canvas中。</p><p>在我们接触HTML 5 CANVAS之前，我们需要了解一些关于HTML5方面的知识。我们介绍一些常见的内容：</p><p>&lt;!DOCTYPE HTML&gt;</p><p>这个是一个标记，告诉浏览器按照标准模式去选软网页，这个简化的标记是HTML 5的标记。</p><p>&lt;html lang=”en”&gt;</p><p>en代表的English，说明html渲染的语言的环境是英文的，中文是设置lang为”zh”。</p><p>&lt;meta charset=”utf-8”\/&gt;</p><p>告诉浏览器用utf-8来编码网页，utf是种世界通用的编码标记，你没必须重写一个，这个是html5页面必选的一个标记之一。</p><p>&lt;title&gt;&lt;\/title&gt;</p><p>告诉浏览器，你网页的名字是什么，这个是构建网站必选的</p><p>&lt;!doctype html&gt;</p><p>&lt;html lang=”en”&gt;</p><p><head></p><p>&lt;title>Hello World</title&gt;</p><p>&lt;meta charset=”utf-8”/&gt;</p><p>&lt;\/head&gt;</p><p>&lt;body&gt;</p><p>&lt;div&gt;Hello World&lt;\/div&gt;</p><p>&lt;\/body&gt;</p><p>&lt;\/html&gt;</p><p>这些都是基本知识，不懂的就去问百度吧。</p><p>上面是使用dom构建的hello world，这里我们看一下第一个Canvas程序，使用Canvas有什么不同。</p><p>HTML</p><p>&lt;canvas id=”myCanvas”width=”500” height=”500”&gt;&lt;/canvas&gt;</p><p>JavaScript</p><p>var myCanvas = document.getElementById(“myCanvas”);</p><p>var context = myCanvas.getContext(“2d”);</p><p>context.fillStyle=”#000000”;</p><p>context.fillText(“Hello World”,100,100);</p><p>context.fill();</p><p>Canvas中的内容是通过JavaScript来处理的。</p><p>分享中难免会有不足的地方，有什么问题或是好的建议，可以在微博中@爱游戏的酱油猫。</p>',selectedNav:'2'}